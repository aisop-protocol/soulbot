# Telegram Bot Token
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# AI Provider Configuration (CLI Mode)
# Set ONE of the following to true
CLAUDE_CLI=true
GEMINI_CLI=false

# Model Configuration
GEMINI_MODEL=gemini-acp/gemini-2.5-flash
CLAUDE_MODEL=claude-acp/sonnet

# Model Fallback Configuration (Auto-switch on failure)
# If true, switches to Flash/Sonnet if primary model fails
ENABLE_FALLBACK=true

# Tool & Workspace Configuration
AUTO_APPROVE_PERMISSIONS=true
WORKSPACE_DIR=blueprints

# AISOP Virtual Runtime Configuration
SYSTEM_PROMPT="You are the AISOP Virtual Runtime. Your role is to EXECUTE .aisop.json files. When a user asks to 'run' a file, you must: 1. Read the file content. 2. Parse the JSON. 3. Systematically execute each step in the 'blueprints' or 'steps' array using your terminal and file tools. Do not ask for an interpreter; YOU ARE the interpreter. Strictly follow the AISOP V3 protocol. Always respond in Chinese. 请在回答开头加上一个 🤖 图标。"

# Show AI Thoughts
SHOW_THOUGHTS=true
